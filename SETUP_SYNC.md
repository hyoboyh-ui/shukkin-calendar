# オンライン同期のセットアップ

出勤カレンダーは、複数端末で同じ記録を見られるように Google Apps Script（GAS）+ スプレッドシートを使ったオンライン同期に対応しています（kakeibo-app と同じ方式）。未設定でも今まで通りローカル（ブラウザ内）保存だけで動作します。

## Step 1: スプレッドシートを作成

1. [Google スプレッドシート](https://sheets.google.com) で「空白のスプレッドシート」を新規作成
2. 名前を「出勤カレンダー」などに変更（シート名は何でもよい）

## Step 2: Apps Script をデプロイ

1. スプレッドシートのメニュー「拡張機能」→「Apps Script」を開く
2. 既存のコードをすべて削除し、`gas/Code.gs` の内容をすべて貼り付ける
3. 保存（Ctrl+S）
4. 右上「デプロイ」→「新しいデプロイ」
   - 種類: **ウェブアプリ**
   - 次のユーザーとして実行: **自分**
   - アクセスできるユーザー: **全員**
5. 「デプロイ」→ Google アカウント認証で「許可」
6. 表示された「ウェブアプリのURL」（`https://script.google.com/macros/s/.../exec`）をコピー

## Step 3: アプリに設定

`src/utils/sync.ts` の `SYNC_URL` に、Step 2 でコピーしたURLを貼り付ける：

```ts
export const SYNC_URL = 'ここにウェブアプリのURLを貼り付ける';
```

保存して `npm run build` → push すれば、以降は自動でオンライン同期が有効になります。

## 仕組みのメモ

- 各日の記録に `updatedAt`（更新時刻）を持たせ、端末間で衝突した場合は新しい方を優先（Last-Write-Wins）
- 入力はネットの有無に関わらず即座にローカルへ反映され、裏で自動的にサーバーへ送信を試みる
- オフライン時は静かに「未同期キュー」に貯め、起動時・アプリ復帰時・設定画面の「今すぐ同期」ボタンで再送信する
- スプレッドシートの `records` シートが唯一のオンラインデータ。手動で編集しても構わない（`date, status, revenue, updatedAt` の列構成）
